import{notFound}from"next/navigation";import{getAdminProducts}from"@/lib/data";import{ProductStatusActions}from"@/components/product-status-actions";import{ProductCoreEditor}from"@/components/product-core-editor";export default async function Edit({params}:{params:Promise<{id:string}>}){const{id}=await params;const p=(await getAdminProducts()).find(x=>x.id===id);if(!p)notFound();return <main><p className="eyebrow">Edycja produktu</p><h1 className="mt-2 text-3xl font-bold">{p.name}</h1><ProductCoreEditor product={p}/><ProductStatusActions id={p.id}/></main>}
