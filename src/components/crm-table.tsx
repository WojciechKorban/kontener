"use client";import{useState}from"react";import type{InquiryRow}from"@/lib/admin-data";const statuses=["NEW","CONTACTED","OFFER_PREPARATION","OFFER_SENT","WON","LOST"];export function CrmTable({initial}:{initial:InquiryRow[]}){const[rows,setRows]=useState(initial);async function change(id:string,status:string){const r=await fetch(`/api/admin/inquiries/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({status})});if(r.ok)setRows(rows.map(x=>x.id===id?{...x,status}:x))}return <div className="overflow-x-auto border bg-white"><table className="w-full min-w-[850px] text-left text-sm"><thead className="bg-[#eeeee9] text-xs uppercase text-[#777]"><tr><th className="p-4">Klient</th><th>Kontakt</th><th>Model</th><th>Data</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr className="border-t align-top" key={x.id}><td className="p-4"><b>{x.name}</b><p className="mt-2 max-w-xs text-xs text-[#777]">{x.message}</p></td><td className="py-4"><a className="block" href={`tel:${x.phone}`}>{x.phone}</a><a className="mt-1 block text-xs text-[#68715a]" href={`mailto:${x.email}`}>{x.email}</a></td><td className="py-4">{x.model}</td><td className="py-4 text-xs">{new Date(x.createdAt).toLocaleDateString("pl-PL")}</td><td className="py-4"><select className="border px-2 py-2 text-[11px] font-bold" value={x.status} onChange={e=>change(x.id,e.target.value)}>{statuses.map(s=><option key={s}>{s}</option>)}</select></td></tr>)}</tbody></table></div>}
