"use client";import{useState}from"react";import{createBrowserClient}from"@supabase/ssr";import{useRouter}from"next/navigation";import{Loader2}from"lucide-react";export function LoginForm(){const router=useRouter();const[loading,setLoading]=useState(false);const[error,setError]=useState("");async function login(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const fd=new FormData(e.currentTarget);if(!process.env.NEXT_PUBLIC_SUPABASE_URL){router.push("/admin");return}const sb=createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);const r=await sb.auth.signInWithPassword({email:String(fd.get("email")),password:String(fd.get("password"))});if(r.error){setError("Nieprawidłowy e-mail lub hasło.");setLoading(false)}else{router.push("/admin");router.refresh()}}return <form onSubmit={login} className="mt-8 space-y-5"><label className="block text-xs font-bold uppercase tracking-wider">E-mail<input className="field mt-2" name="email" type="email" required/></label><label className="block text-xs font-bold uppercase tracking-wider">Hasło<input className="field mt-2" name="password" type="password" required/></label>{error&&<p className="text-sm text-red-700">{error}</p>}<button className="btn btn-dark w-full" disabled={loading}>{loading&&<Loader2 size={16} className="animate-spin"/>}Zaloguj</button></form>}
