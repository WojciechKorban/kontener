"use client";import{useState}from"react";import{Save,Loader2}from"lucide-react";import type{Product}from"@/lib/types";export function ProductCoreEditor({product}:{product:Product}){const[busy,setBusy]=useState(false);const[done,setDone]=useState(false);async function save(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setDone(false);const fd=new FormData(e.currentTarget);const r=await fetch(`/api/admin/products/${product.id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(fd))});setBusy(false);setDone(r.ok)}return <form onSubmit={save} className="my-7 grid gap-5 border bg-white p-6 sm:grid-cols-2"><label className="text-xs font-bold uppercase">Nazwa<input name="name" className="field mt-2" defaultValue={product.name}/></label><label className="text-xs font-bold uppercase">Slug<input name="slug" className="field mt-2" defaultValue={product.slug}/></label><label className="text-xs font-bold uppercase">Powierzchnia<input name="area" type="number" className="field mt-2" defaultValue={product.area}/></label><label className="text-xs font-bold uppercase">Cena od<input name="price_from" type="number" className="field mt-2" defaultValue={product.priceFrom}/></label><label className="text-xs font-bold uppercase sm:col-span-2">Krótki opis<textarea name="short_description" className="field mt-2" rows={3} defaultValue={product.shortDescription}/></label><label className="text-xs font-bold uppercase sm:col-span-2">Pełny opis<textarea name="description" className="field mt-2" rows={6} defaultValue={product.description}/></label><div className="flex items-center gap-4 sm:col-span-2"><button disabled={busy} className="btn btn-dark">{busy?<Loader2 className="animate-spin" size={16}/>:<Save size={16}/>}Zapisz zmiany</button>{done&&<span className="text-sm text-[#567046]">Zapisano.</span>}</div></form>}
