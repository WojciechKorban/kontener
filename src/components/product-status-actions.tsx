"use client";import{useState}from"react";import{useRouter}from"next/navigation";export function ProductStatusActions({id}:{id:string}){const router=useRouter();const[busy,setBusy]=useState(false);async function change(status:string){setBusy(true);const r=await fetch(`/api/admin/products/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({status})});setBusy(false);if(r.ok)router.refresh()}return <div className="flex flex-wrap gap-3"><button disabled={busy} onClick={()=>change("DRAFT")} className="btn btn-outline">Zapisz jako draft</button><button disabled={busy} onClick={()=>change("ARCHIVED")} className="btn btn-outline">Archiwizuj</button><button disabled={busy} onClick={()=>change("PUBLISHED")} className="btn btn-dark">Publikuj</button></div>}
