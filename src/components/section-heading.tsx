export function SectionHeading({ eyebrow, title, text, light = false }: { eyebrow: string; title: string; text?: string; light?: boolean }) { return <div className="mb-12 grid gap-5 md:grid-cols-2 md:items-end"><div><p className={`eyebrow ${light ? "!text-white/55" : ""}`}>{eyebrow}</p><h2 className="display mt-4 text-5xl md:text-6xl">{title}</h2></div>{text && <p className={`max-w-lg text-sm leading-7 ${light ? "text-white/60" : "text-[#666b63]"}`}>{text}</p>}</div>; }
