import {createServerClient}from"@supabase/ssr";import{NextResponse,type NextRequest}from"next/server";export async function proxy(req:NextRequest){if(!process.env.NEXT_PUBLIC_SUPABASE_URL)return NextResponse.next();let res=NextResponse.next({request:req});const sb=createServerClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,{cookies:{getAll:()=>req.cookies.getAll(),setAll(cs){cs.forEach(c=>req.cookies.set(c.name,c.value));res=NextResponse.next({request:req});cs.forEach(c=>res.cookies.set(c.name,c.value,c.options))}}});const{data}=await sb.auth.getUser();if(!data.user&&!req.nextUrl.pathname.startsWith("/admin/login")){const u=req.nextUrl.clone();u.pathname="/admin/login";return NextResponse.redirect(u)}return res}export const config={matcher:["/admin/:path*"]};
